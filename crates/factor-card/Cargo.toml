[package]
name = "factor-card"
version = "0.1.0"
edition = "2021"
rust-version = "1.77"

[lib]
name = "factor_card"

[dependencies]
serde_json = "1.0"
tiny-skia = { version = "0.11", default-features = false, features = ["std", "simd", "png-format"] }
ab_glyph = "0.2"

[dev-dependencies]
ureq = "2.10"
png = "0.17"
